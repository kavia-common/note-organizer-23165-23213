# Frontend environment variables for Ocean Notes (Qwik)
# Copy to .env and adjust values for your environment.

# Base URL for backend API (e.g., http://localhost:8000 or empty for same origin)
VITE_API_BASE=
